@{
    ViewBag.Title = "ViewInventoryForOutlet";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>ViewInventoryForOutlet</h2>

<p>
    Please enter outletID
</p>
@{
            if(TempData["Results"] != null)
            {
                <div class="alert alert-danger">@TempData["Results"] </div>
            }
        }
@using (Html.BeginForm("InventoryForOutlet", "Shop", FormMethod.Get))
{
    <div>
        <fieldset>
            <legend>Outlet Information</legend>

            <div class="editor-label">
                @Html.Label("ID", "ID")
            </div>
            <div class="editor-field">
                @Html.Editor("outletID")
            </div>

            <p>
                <input type="submit" value="Search" />
            </p>
        </fieldset>
    </div>

}